#if !defined(L2MEMORY)
    #define L2MEMORY 1048576
#endif

#if !defined(CACHE_BLOCK_SIZE)
    #define CACHE_BLOCK_SIZE 512
#endif

#if !defined(DATA_BLOCK_SIZE)
    #define DATA_BLOCK_SIZE 512000
#endif

#if !defined(MEMORY_BUSS_MHZ)
    #define MEMORY_BUSS_MHZ 400
#endif


#if !defined(MEMORY_BUSS_LATENCY)
    #define MEMORY_BUSS_LATENCY 30
#endif
